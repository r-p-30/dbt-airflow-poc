# --- Core packages ---
astronomer-cosmos==1.11.0

# --- dbt core + adapter ---
dbt-core==1.10.0
dbt-snowflake==1.10.0

# --- Pin FastAPI & dependencies for Airflow 3.1 compatibility ---
fastapi==0.117.0
starlette==0.48.0
pydantic==2.8.2

# --- Optional: useful Airflow providers if you interact with Snowflake or dbt Cloud ---
# apache-airflow-providers-snowflake
# apache-airflow-providers-dbt-cloud
